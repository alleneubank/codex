load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "exec",
    build_script_data = ["//codex-rs:fork-version.txt"],
    build_script_env = {
        "CODEX_FORK_VERSION_PATH": "$(execpath //codex-rs:fork-version.txt)",
    },
    crate_name = "codex_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
